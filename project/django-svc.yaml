apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
    app: wordpress
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 31111
  selector:
    app: wordpress
    tier: frontend

